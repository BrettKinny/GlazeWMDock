## .NET / Visual Studio
bin/
obj/
.vs/
*.user
*.suo
AppPackages/
BundleArtifacts/
*.appx
*.msix
*.appxbundle
*.msixbundle
*.msixupload
*.cer
*.pfx
_ReSharper*/
*.DotSettings.user

## Secrets / signing material (insurance — none are tracked today)
*.snk
*.pem
*.p12
*.key
.env
.env.*

# NOTE: Unlike the stock C# .gitignore, do NOT ignore these — the Windows App
# SDK needs them to deploy the package on a fresh clone:
#   !**/Properties/launchSettings.json
#   !*.pubxml
